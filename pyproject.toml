[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tp-ia-2"
version = "0.1.0"
description = "Projet Python dans devcontainer"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
]
